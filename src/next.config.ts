// This file is intentionally left empty.
// Configuration is managed in next.config.js to avoid potential conflicts.
// Please make all Next.js configuration changes in next.config.js.

import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Ensure this object remains empty or only contains comments
  // if next.config.js is the primary configuration file.
};

export default nextConfig;
